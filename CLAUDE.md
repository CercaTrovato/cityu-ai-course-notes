# CLAUDE.md — CityU 课程笔记库（根级指导文件）

> 本文件面向 **Claude Code**。工具无关的同等规则见同目录 [AGENTS.md](AGENTS.md)。
> 两份文件内容对齐、各自独立自洽，任一份被单独复制走都应能独立使用。

---

## 1. 这个库是什么

`D:\上课资料\CityU` 是一个 **Obsidian vault**，用途是把 CityU 各门课的原始材料（讲义 PDF/PPTX、课堂录音转录、数据、代码）转成**可自学的中文精读笔记**，并在 Notion 上追踪学习进度。

**核心读者假设**：读笔记的人**没有听过课**。他只有 AI / 机器学习的基础知识，其余（哲学、伦理学、法律、会计、金融）**零基础**。笔记必须让他仅凭笔记就能掌握课堂内容。

这条假设决定了后面所有规则。任何"读者应该知道"的默认，都要显式写出来或就地解释。

---

## 2. 目录约定

```
CityU/                              ← vault 根
├─ CLAUDE.md / AGENTS.md            ← 本文件（通用方法论）
├─ README.md                        ← vault 首页总索引
├─ _meta/                           ← 跨课程公共层
│   ├─ 笔记模板.md                   ← module 笔记的标准骨架（写笔记前必读）
│   ├─ 笔记质量规范.md               ← ★ 可量化的质量门槛（标 v0.9 前必须过脚本）
│   ├─ tools/note_quality.py         ← 质量量表脚本（并入原七项自检）；tools/link_check.py 全库链接检查
│   ├─ 材料处理规则.md                ← 各类材料怎么读、怎么转（扩展接口在此）
│   ├─ 转录处理规则.md                ← 课堂录音转录的专项规则（原理层）
│   ├─ tools/transcript_check.py     ← 转录融合验收：scan 登记 / audit 12 项（融合后必须 PASS）
│   ├─ tools/safe_write.py · integrity_check.py · backup_vault.py ← ★ 文件安全三件套（原子写 / 快照比对 / 备份），见「文件安全协议」
│   ├─ Notion进度看板.md              ← Notion 两个库的字段定义与回写规则
│   ├─ 术语总表.md                    ← 全 vault 双语术语，跨课去重
│   ├─ Obsidian使用说明.md
│   └─ 设计规格-笔记体系.md            ← 本体系的设计依据，改规则前先读
│
├─ .claude/skills/transcript-merge/  ← ★ 转录融合 skill（执行层 SOP + 模板 + ASR 词典 + 任务单模板）；合并转录一律按它做
│
└─ <课程英文目录名>/                  ← 每门课一个，如 AI_Ethics_and_Regulations
    ├─ CLAUDE.md / AGENTS.md        ← 课程级：本课特有的规则与背景
    ├─ 00-课程总览.md                ← MOC（Map of Content），本课入口
    ├─ _prep/                       ← 前置资料
    │   ├─ 课程前置资料.md            ← 官方调研 × Course Outline 对校
    │   └─ 口碑与攻略.md              ← 社媒/论坛口碑，标注可信度
    ├─ _meta/
    │   ├─ 知识层级台账.md            ← ★ 连贯性的核心机制
    │   ├─ 术语表.md                  ← 本课术语（中英对照）
    │   ├─ 考点库.md                  ← 按 module 汇总的潜在考点
    │   └─ 作业与DDL.md               ← 作业、截止日期、提交要求
    ├─ transcripts/                 ← ★ 课堂录音转录（Notta 导出）
    │   └─ M01-transcript.txt ...
    ├─ notes/
    │   └─ M01-<主题>.md ...          ← module 笔记正文
    └─ （原始课件保持原位，不移动、不改名、不修改）
```

**硬约束：绝不移动、改名、删除或修改任何原始课件文件。** 它们是唯一事实来源，且用户可能在别处引用了路径。

---

## 3. 核心机制一：前置知识台账

这是保证"读第 N 讲时知识连贯、不出现陌生概念"的**机制**，不是建议。

每门课的 `_meta/知识层级台账.md` 有两部分：

**L0 入场基线** — 显式列出读者进门时已有的知识（例：监督学习、训练集、模型、准确率）。不在 L0 里的东西，就是需要解释的东西。这把"零基础"从模糊形容词变成可对照的线。

**L1 概念登记表** — 每个概念一行：`概念 | English | 首现 module | 一句话定义 | 依赖的前置概念`。

**写 M(N) 笔记时的强制流程：**

1. 写之前先读台账
2. 正文里每出现一个概念，判断：
   - 在 L0 里 → 直接用，不解释
   - 在 L1 里且**首现 module ≤ N** → 直接用，并用 `[[M0X-主题#小节]]` 回指，附**一句话唤醒**（不要求读者真的跳回去）
   - 在 L1 里但**首现 module > N** → 说明这是后面的内容，只给一句话预告，不展开
   - **不在表里** → 必须在本 module 就地完整展开（是什么 / 为什么需要它 / 例子），然后**登记回 L1**
3. 写完后更新台账、`_meta/术语表.md`、`_meta/考点库.md`

**验收标准**：把 M01…M(N) 的笔记连起来读，不应出现任何"这个词前面没说过、这里也没解释"的情况。写完自查一遍。

---

## 4. 核心机制二：正文按学习逻辑重排

课件的页码顺序是**讲课顺序**，常常不是**学习顺序**（典型：前十几页是课程行政信息，中间穿插与主线无关的宣传页，同一概念分散在不同段落）。

**规则**：正文按"从零学会"的最优顺序重组，并在笔记第 8 节提供 `笔记小节 ↔ 讲义页码` 双向映射表。

重排要在第 1.3 小节**说明理由**（"课件是 A→B→C 四段拼接，我改成 B→C→A 因为……"），让读者知道自己看的不是原顺序，并能随时对回讲义。

### ⚠️ 重排 ≠ 挑着写：每一页都要有实质讲解

**重排改变的是顺序，不是覆盖范围。**

> **验收标准：一个没听过课的人，只靠这份笔记预习，就能学会讲义上的全部知识。**

1. **讲义每一页都必须在 §2 正文里有实质讲解**，不能只出现在 §8 映射表。**映射表是索引，不是讲解。**
2. 例外只有两类且必须在 §8 标出：**封面页**、**学习目标页**（后者仍要在对应小节开头写一句「本节对应学习目标：…」）
3. **「课上略过了」不是跳过的理由**——正文照样展开，只在 §8「课堂覆盖」列标 `⏭️` 并在 §9.1 说明。读者假设是"没听过课的人"，**课上略过恰恰意味着这部分只能靠笔记学**
4. **无转录时更严格**：讲义是唯一信源，每一页都得吃透

> ❗ 这条来自一次真实事故：AC6761 M01 把 19 页（交易 2–11 的逐笔分析）只登记进映射表、正文没展开，理由是"课上没讲到"——而那正是期中考内容，**预习者完全学不到**。

验收用脚本，见 [`_meta/对抗自检清单.md`](_meta/对抗自检清单.md) 第 1–4 项。

---

## ★ 文件安全协议（2026-09-17 起，任何模型改 vault 文件都必须遵守）

> 起因：2026-09-16 / 17 三篇笔记（IS6400 M03、AC6761 M01、AC6761 M03）在"脚本整读整写 + Remotely Save 同步在跑"时被截断成半截文件，并被同步到远端覆盖了完整版；AC6761 M01 的 §7 后半到 §9 因此永久丢失。

1. **写文件必须原子**：一律 `from safe_write import atomic_write`（`_meta/tools/safe_write.py`），它先写临时文件再 `os.replace`，并拒绝把文件缩短到旧文件 70% 以下。**禁止** `open(path, 'w').write(...)` 直接写笔记；Edit 工具可以用（它本身是整体替换）。
2. **开工前快照、收工后核对**：任务开始 `integrity_check.py snapshot <标签>`（并跑一次 `backup_vault.py`），结束 `integrity_check.py verify`；任何 ✗ 都要在回执里报告。改动清单里出现"我没打算改的文件"也要报告。
3. **发现文件损坏：停下、报告、不重建。** 不许凭记忆或凭上下文"重写"一篇笔记去顶替损坏的文件——那会产出看起来像原文但不是原文的东西。恢复来源只能是：备份 zip、git、Obsidian「文件恢复」快照、或会话 transcript 里完整的 Read 记录。
4. **agent 改文件期间关闭同步**：Remotely Save 的自动同步（定时 / 保存后同步）必须关掉，只在没有 agent 运行时手动同步；手机端同理。这一条由用户执行，agent 开工前在回执里确认。
5. 脚本用 Write 工具写成 `.py` 再运行，不用 shell heredoc（会吃反斜杠与中文，2026-09-17 一天内出错四次）。

命令速查（vault 根）：
```
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/backup_vault.py
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/integrity_check.py snapshot 开工
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/integrity_check.py verify [--allow 有意重写的文件]
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/safe_write.py --check <文件>
```

## 5. 核心机制三：讲义 × 转录 双源比对

用户每节课都用 **Notta** 录音并生成完整转录。转录不是讲义的补充说明，它是**独立的一等材料**：

- **讲义** = 教授准备讲什么（结构、定义、框架）
- **转录** = 教授实际讲了什么（例子、强调、跳过、考试暗示、课堂问答）

**两者的差集才是最高价值的信息：**

| 情况 | 价值 | 笔记里怎么处理 |
|---|---|---|
| 课件有 + 课上讲了 | 核心内容 | 正文主体 |
| 课件有 + **课上略过** | 考试权重低 | 正文保留，但标注「课上略过」，让读者能降低复习优先级 |
| **课件没有 + 课上讲了** | **最高**（只有到场的人才有） | 正文重点，标 🎙️ |
| 教授原话强调（"this will be on the exam"、"you must remember"） | 直接考点 | 进考点库，可信度标「教授明示」 |
| 课堂问答与讨论 | 真实的误解来源 | 填进正文的「常见误解」格 |

详细规则见 [`_meta/转录处理规则.md`](_meta/转录处理规则.md)（原理）与 skill [`.claude/skills/transcript-merge/SKILL.md`](.claude/skills/transcript-merge/SKILL.md)（执行步骤、模板、验收），**处理转录前必读**；融合完成的唯一标准是 `_meta/tools/transcript_check.py audit` PASS + `note_quality.py` 不回退 + `link_check.py` 0。其中最关键的一条：

> **ASR 专名错误一律以讲义的书面拼写为准校正**。Notta 对英文哲学/法律专名错误率高（`Kantianism`→`Contianism`、`deontology`→`the ontology`、`Bentham`→`Benthem`）。转录里听不清的术语标 `[?]`，**不要猜一个填上**。

**两阶段产出**：
- **v0.9** — 只有讲义、转录未到位。正常出笔记，frontmatter 标 `transcript: pending`，第 9 节写明「本讲尚无转录，课堂补充待回填」
- **v1.0** — 转录到位后合并，补齐 🎙️ 标记的内容、升级第 6 节考点可信度、frontmatter 改 `transcript: merged`

不要为了等转录而不写笔记。v0.9 本身就有用，后续合并成本很低。

---

## 6. 笔记模板

统一 9 节骨架，定义在 [`_meta/笔记模板.md`](_meta/笔记模板.md)。**写任何 module 笔记前先读那个文件**，不要凭记忆复现。

概要：`0 速览 → 1 知识衔接 → 2 正文 → 3 一图看懂 → 4 速查表 → 5 双语术语卡 → 6 考点与答题框架 → 7 自测 → 8 页码映射 → 9 延伸与勘误`

正文里每个概念走**七格微结构**：
**是什么 → 为什么需要它 → 课件原例 → 🎙️ 课堂补充 → 换个说法 → 常见误解 → 与其他概念的关系**

（无转录时第 4 格写「待转录补充」，不要删掉这一格——留着它才知道哪里还欠债。）

---

## 7. 写作规范

**语言**
- 中文为主体。术语首次出现写 `中文（English Term）`，之后视情况用中文或英文
- 考试用英文作答的课程，关键定义**保留英文原句**并给中文翻译——考场上要写的是英文
- 中英混排时中文与英文/数字之间留空格

**忠实性（最重要）**

必须区分并显式标注**四种来源**：

| 来源 | 标注方式 |
|---|---|
| 讲义原文/原例 | 直接引用 + `（讲义 p.XX）` |
| 课堂转录 | `🎙️ 课堂补充` + 时间戳（若有） |
| 笔记补充（你写的解释、类比、例子） | `💡 换个说法（笔记补充）` |
| 外部资料 | `🔗` + 来源 + 获取日期 |

- **不编造**。材料里没有的内容不要伪装成材料里有。不确定的地方写"课件此处未展开"
- 转录里教授的原话，如果要当考点依据，**引用原话**而不是转述
- 课件本身有错误、口径不一致、或明显是模板占位内容，**如实写进第 9 节勘误**。不要粉饰——读者需要知道哪些页是低信噪比的，好分配复习时间

**Obsidian 兼容**
- 每个笔记文件带 YAML frontmatter（字段见模板）
- 内部引用一律用 `[[双链]]`，不用相对路径 Markdown 链接
- **公式一律 LaTeX**（`$…$` / `$$…$$`），绝不用代码块或行内代码写公式——Obsidian 不会渲染，读者看到的是乱码。表格里的竖线写 `\mid`。细则见 [[笔记模板]]「★ 公式与代码的写法」
- **每个小节写给第一次见这个概念的人**：先人话再定义、术语就地解释、公式配符号表和数字例子、代码配逐行说明、不以公式/代码/表格开头。细则见 [[笔记模板]]「★ 预习可读性」，验收见 [[对抗自检清单]] 第 11 项
- **⚠️ 质量硬门槛（2026-09-16 起）**：笔记标 `v0.9` 之前必须跑 `_meta/tools/note_quality.py <笔记> --strict --sections`，**L 合规 0 问题 + G 硬门槛全过**（§2 ≥ 250 字/内容页、一节 ≤ 1.6 个术语、≤ 2.5 页、首元素段落、公式符号有着落、有公式必有数字例子、代码三件套、「所以呢」覆盖 ≥ 0.80），新写 / 整篇重写的笔记还要加 frontmatter `quality_spec: v1` 并过 E 元素清单；`_meta/tools/link_check.py` 全库 0 问题。规则、阈值与校准依据见 [[笔记质量规范]]。**起因**：IS6400 M03 / M04 通过了全部既有检查却明显不如 M02 透彻——形式检查抓不住"薄"，透彻度必须量
- **⚠️ 两条链接硬规则（2026-09-10 全库返工 251 + 407 处后定下）**：
  1. **同名文件必须带目录**。五门课各有一份 `00-课程总览` / `术语表` / `考点库` / `作业与DDL` / `知识层级台账` / `课程前置资料` / `数据集卡片`，裸写 `[[术语表]]` 在 Obsidian 里会随机落到别的课。写法：`[[Business_Data_Analytics/_meta/术语表|术语表]]`（路径 + 别名，显示不变）。只有根目录文件（`CLAUDE` / `README` / `术语总表` / 各规则文件）可以裸写。
  2. **标题锚点写标题原文，不写 GitHub 式 slug**。Obsidian 按标题文字匹配，`#2-5-2-四条假设` 这种连字符写法**一个都匹配不上**（会静默跳到文件顶部）。写法：`[[M02-预测分析-线性回归#2.5.2 四条假设（讲义 p.17–18，⭐ 必考）|M02 §2.5.2]]`——锚点复制标题全文（去掉 `#` `|` `^` `[` `]`），别名给读者看。表格单元格里管道要转义成 `\|`。
  - 自检脚本见 [[对抗自检清单]] 第 9b 项。
- 图表用 Mermaid 或 Markdown 表格重画，不嵌位图（保证可搜索、可编辑、库体积小）
- 自测题答案用 `<details><summary>` 折叠

---

## 8. 材料处理

各类材料的提取方式与产出物，见 [`_meta/材料处理规则.md`](_meta/材料处理规则.md)。该文件用一张状态表管理类型，**新增材料类型时往表里加一行 + 补一节规则即可**，不要改本文件。

当前状态速览：`.pdf` 已启用；`.docx` 已启用；**课堂转录已启用**（见 `_meta/转录处理规则.md`）；`.pptx` 待装 `python-pptx`；`.csv/.xlsx`、`.py/.ipynb`、原始音视频、网页/论文为**预留槽位**（规则待定，遇到时先补规则再动手）。

---

## 9. 标准工作流

新增一个 module 笔记：

1. **定位材料** — 找到讲义 + `transcripts/` 下对应转录，确认页数/大小
2. **读官方口径** — 读该课 `_prep/课程前置资料.md`，明确 ILO 与考核方式（决定笔记侧重）
3. **提取讲义全文** — 按 `_meta/材料处理规则.md` 转文本，存到 scratchpad，不落到 vault 里
4. **视觉复核** — 纯图片页（文本提取为空的页）用 Read 工具看图，补回内容
5. **处理转录** — 按 skill `.claude/skills/transcript-merge/`：scan 登记 → 通读对齐 → 四态回填 🎙️ 格 → 结构块 → 跨文件回写 → `transcript_check.py audit` PASS
6. **查台账** — 读 `_meta/知识层级台账.md`，列出本讲的"已有 / 新增 / 预告"三类概念
7. **重排大纲** — 定学习逻辑顺序，记录与讲义页码的映射
8. **写正文** — 按模板 9 节，七格微结构
9. **回写元数据** — 更新台账 L1、课程术语表、`_meta/术语总表.md`、考点库；有作业则登记 `作业与DDL.md`
10. **同步 Notion** — 更新该 module 在进度库的勾选状态与笔记路径；新作业写进 DDL 库。规则见 `_meta/Notion进度看板.md`
11. **★ 对抗自检** — 跑 [`_meta/对抗自检清单.md`](_meta/对抗自检清单.md) 的 **12 项 + 11c 质量量表**（`_meta/tools/note_quality.py` G 硬门槛全过、`link_check.py` 0 问题），**全过才算完成**；任一 ✗ 停在 `draft`

---

## 9.1 对抗自检（写完不等于完成）

**写完一篇笔记后，换一个身份——一个想证明这篇笔记不合格的审查者——回头攻击自己的产出。**

完整清单见 [`_meta/对抗自检清单.md`](_meta/对抗自检清单.md)，四组共 12 项：

| 组 | 查什么 | 关键项 |
|---|---|---|
| **A · 覆盖** | 用脚本验，不许目测 | ① **每页都在正文有讲解**（不只在映射表）② 课上略过 ≠ 不写 ③ 低文本页全部视觉复核 ④ OLE 嵌入对象已提取 |
| **B · 真实性** | 最容易出事 | ⑤ **无转录时不许出现"教授说"** ⑥ 每条来源可回溯 ⑦ **无转录时置信度 🟡 封顶** ⑧ 数字全部可核 |
| **C · 连贯性** | | ⑨ 无悬空概念 ⑩ 台账已回写 |
| **D · 目标达成** | 最重要 | ⑪ **随机抽 3 页做预习可用性抽测** ⑫ **反方视角**：最弱的一节是哪节 / 哪个考点我答不上 / 哪些是推断 |

**为什么需要这一步**：本库已发生过的失败都有同一个形状——**"看起来做完了"**。

| 事故 | 后果 |
|---|---|
| 跳过纯图片页 | 漏掉整篇论文 + 两张关键表 |
| 19 页只进映射表 | 预习者学不到，而那是期中考内容 |
| 把"有 notes_slide"当"有备注" | 106/117 的结论，实际 6/117 |
| 把"时间戳连续"当"录音完整" | 漏判开头 15 分钟永久缺失 |
| 按文本长度判定分隔页 | 差点漏掉整页理论出处 |

⚠️ **简报里不许写"已全部检查通过"了事——报数字、报页码、报结论。**

---

## 10. 扩展接口

**加一门新课**：在 vault 根建课程目录（英文名，下划线分隔），复制上面的子结构，写课程级 `CLAUDE.md` / `AGENTS.md`，先做 `_prep/课程前置资料.md` 再动笔记，并在 Notion 进度库补齐该课的 module 行。

**加一种新材料类型**：只改 `_meta/材料处理规则.md`——加一行状态表 + 一节规则（提取命令、产出物形态、质量检查点）。规则写完再开始处理该类型材料。

**改笔记模板**：改 `_meta/笔记模板.md`，并在 `_meta/设计规格-笔记体系.md` 里记录改动理由。已写好的旧笔记不强制回改，除非用户要求。

---

## 11. Claude Code 专属

**本机可用工具**（已验证）
- `pdftotext -layout`（poppler，`/d/poppler`）、`pdfinfo` — PDF 首选
- Python `/d/python/python`，已装 `pypdf`、`python-docx`、**`python-pptx` 1.0.2**；未装 `pdfplumber`、`PyMuPDF`
- **Read 工具可直接按页读 PDF 并以图像呈现**（`pages` 参数，单次最多 20 页）——这是复核纯图片页的正确方法，不需要额外的图像提取工具
- `opencli` 1.8.6 — 社媒抓取。`rednote` 子命令可用（`search` / `note` / `comments` / `user`），但**需要用户先在 Chrome 登录**，不要尝试代为登录
- **Notion MCP 已连接**（server id `3d0e10e6-…`）。可用 `notion-fetch` / `notion-query-data-sources` / `notion-create-pages` / `notion-update-page` 等
- `WebSearch` / `WebFetch` — 官方资料调研。注意 WebSearch 基本不覆盖小红书内容

**Notion 现状**
- 工作区里的 `📚 课表与每日安排`（`collection://6be2f4c0-e2de-4d26-b9b3-dc6cea32313d`）是 2025-11 的旧本科课表，**与本库无关，不要改动它**
- 本库使用的两个库见 `_meta/Notion进度看板.md`

**约定**
- 中间产物（提取的 txt、下载的 PDF）一律放 scratchpad，**不要落进 vault**
- PDF 文本提取会把项目符号渲染成 `\ufffd`，替换成 `-` 再处理
- Windows 下 Python 默认 GBK 输出会炸，脚本前加 `PYTHONIOENCODING=utf-8`
- 本库**不是 git 仓库**，用户自行备份。不要 `git init`

**回复语言**：一律简体中文（用户全局规则）。
