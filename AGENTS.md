# AGENTS.md — CityU 课程笔记库（根级指导文件 · 工具无关版）

> 本文件面向**任何 AI 助手**（ChatGPT / Gemini / Cursor / Copilot / 本地模型等）。
> 不假设你有 shell、文件系统或联网能力——能做多少做多少，做不到的显式说明。
> Claude Code 专用版见同目录 `CLAUDE.md`，两份规则等价。

---

## 1. 任务是什么

把 CityU 各门课的原始材料，转成**可自学的中文精读笔记**，并在 Notion 上追踪学习进度。

**唯一的读者假设**：读笔记的人**没听过课**。他只懂 AI / 机器学习基础，哲学、伦理学、法律、会计、金融**零基础**。他必须仅凭笔记就学会课堂内容。

这条假设是所有规则的来源。凡是"读者应该知道"的默认，都要显式解释。

---

## 2. 你会被交给什么、要交出什么

**输入**（可能只有一部分）：
- 讲义（PDF / PPTX / DOCX）
- **课堂录音转录**（用户录音后用本地 Whisper 生成，放在 `<课程>/transcripts/`；2026-09-17 前为 Notta）
- 该课的 `_prep/课程前置资料.md`（课程目标与考核方式）
- 该课的 `_meta/知识层级台账.md`（前置知识登记表）

**输出**：一个 `.md` 笔记文件（9 节固定骨架，见第 6 节），外加对台账、术语表、考点库的增量更新，以及 Notion 进度看板的状态更新。

---

## 3. 核心机制一：前置知识台账

保证"读第 N 讲时不出现陌生概念"的**机制**。

台账（`_meta/知识层级台账.md`）有两部分：

- **L0 入场基线**：读者进门时已有的知识清单。不在 L0 的东西就要解释。
- **L1 概念登记表**：`概念 | English | 首现 module | 一句话定义 | 依赖`。

**写 M(N) 笔记时按这个判断每一个概念：**

| 情况 | 怎么处理 |
|---|---|
| 在 L0 里 | 直接用，不解释 |
| 在 L1 里，首现 ≤ N | 直接用 + `[[回指链接]]` + **一句话唤醒**（别让读者非跳回去不可） |
| 在 L1 里，首现 > N | 只给一句话预告，标明"第 X 讲展开"，不提前展开 |
| **不在表里** | **必须就地完整展开**（是什么 / 为什么 / 例子），然后登记回 L1 |

写完做一次连贯性自查：把 M01 到 M(N) 的笔记连起来读，不该出现"没定义过、这里也没解释"的词。

如果你**拿不到台账文件**（比如只被喂了一份 PDF），就在笔记开头新建一节"本讲新引入概念清单"，并明确告诉用户台账缺失、需要人工合并。

---

## 4. 核心机制二：正文按学习逻辑重排

课件顺序是讲课顺序，通常不是学习顺序（常见问题：开头一堆课程行政信息、宣传页穿插主线、同一概念分散多处）。

**规则**：正文按"从零学会"的最优顺序重组，并提供 `笔记小节 ↔ 讲义页码` 映射表（第 8 节）。在第 1.3 小节说明重排理由，让读者知道顺序变了、且能对回原讲义。

### ⚠️ 重排 ≠ 挑着写：每一页都要有实质讲解

**重排改变的是顺序，不是覆盖范围。**

> **验收标准：一个没听过课的人，只靠这份笔记预习，就能学会讲义上的全部知识。**

1. **讲义每一页都必须在第 2 节正文里有实质讲解**，不能只出现在第 8 节映射表。**映射表是索引，不是讲解。**
2. 例外只有两类且必须在第 8 节标出：**封面页**、**学习目标页**（后者仍要在对应小节开头写一句「本节对应学习目标：…」）
3. **「课上略过了」不是跳过的理由**——正文照样展开，只在第 8 节「课堂覆盖」列标 `⏭️` 并在 9.1 说明。读者假设是"没听过课的人"，**课上略过恰恰意味着这部分只能靠笔记学**
4. **无转录时更严格**：讲义是唯一信源，每一页都得吃透

> ❗ 这条来自一次真实事故：AC6761 M01 把 19 页（交易 2–11 的逐笔分析）只登记进映射表、正文没展开，理由是"课上没讲到"——而那正是期中考内容，**预习者完全学不到**。

验收方法见 `_meta/对抗自检清单.md` 第 1–4 项。

---

## ★ 文件安全协议（2026-09-17 起，任何模型改 vault 文件都必须遵守）

> 起因：2026-09-16 / 17 三篇笔记（IS6400 M03、AC6761 M01、AC6761 M03）在"脚本整读整写 + Remotely Save 同步在跑"时被截断成半截文件，并被同步到远端覆盖了完整版；AC6761 M01 的 §7 后半到 §9 因此永久丢失。

1. **写文件必须原子**：一律 `from safe_write import atomic_write`（`_meta/tools/safe_write.py`），它先写临时文件再 `os.replace`，并拒绝把文件缩短到旧文件 70% 以下。**禁止** `open(path, 'w').write(...)` 直接写笔记；Edit 工具可以用（它本身是整体替换）。
2. **开工前快照、收工后核对**：任务开始 `integrity_check.py snapshot <标签>`（并跑一次 `backup_vault.py`），结束 `integrity_check.py verify`；任何 ✗ 都要在回执里报告。改动清单里出现"我没打算改的文件"也要报告。
3. **发现文件损坏：停下、报告、不重建。** 不许凭记忆或凭上下文"重写"一篇笔记去顶替损坏的文件——那会产出看起来像原文但不是原文的东西。恢复来源只能是：备份 zip、git、Obsidian「文件恢复」快照、或会话 transcript 里完整的 Read 记录。
4. **agent 改文件期间关闭同步**：Remotely Save 的自动同步（定时 / 保存后同步）必须关掉，只在没有 agent 运行时手动同步；手机端同理。这一条由用户执行，agent 开工前在回执里确认。
5. 脚本用 Write 工具写成 `.py` 再运行，不用 shell heredoc（会吃反斜杠与中文，2026-09-17 一天内出错四次）。

命令速查（vault 根）：
```
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/backup_vault.py
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/integrity_check.py snapshot 开工
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/integrity_check.py verify [--allow 有意重写的文件]
PYTHONIOENCODING=utf-8 /d/anaconda3/python.exe _meta/tools/safe_write.py --check <文件>
```

## 5. 核心机制三：讲义 × 转录 双源比对

用户每节课都录音并生成完整转录。**转录是独立的一等材料，不是讲义的附注。**

- **讲义** = 教授准备讲什么（结构、定义、框架）
- **转录** = 教授实际讲了什么（例子、强调、跳过、考试暗示、课堂问答）

**两者的差集才是最高价值的信息：**

| 情况 | 价值 | 笔记里怎么处理 |
|---|---|---|
| 课件有 + 课上讲了 | 核心内容 | 正文主体 |
| 课件有 + **课上略过** | 考试权重低 | 正文保留，但标注「课上略过」，让读者能降低复习优先级 |
| **课件没有 + 课上讲了** | **最高**（只有到场的人才有） | 正文重点，标 🎙️ |
| 教授原话强调（"this will be on the exam"） | 直接考点 | 进考点库，可信度标「教授明示」，**引用原话**而非转述 |
| 课堂问答与讨论 | 真实的误解来源 | 填进正文的「常见误解」格 |

**必须遵守的转录处理规则**（原理见 `_meta/转录处理规则.md`；**执行步骤、模板与验收见 `.claude/skills/transcript-merge/SKILL.md`——Codex 等不自动加载 skill 的 agent 也必须先读完它再动手**；完成标准 = `_meta/tools/transcript_check.py audit` PASS；2026-09-18 起默认走 SKILL.md §11 分片并行模式，分片代理只产出 patch / findings、由 `merge_apply.py` 一次写入）：

1. **专名一律以讲义的书面拼写为准校正**。语音转写对英文哲学/法律专名错误率很高（`Kantianism`→`Contianism`、`deontology`→`the ontology`、`Bentham`→`Benthem`）。先用讲义术语表过一遍转录
2. **听不清的术语标 `[?]`，不要猜一个填上**
3. 转录里的口语重复、语气词、跑题闲聊不进笔记
4. 引用教授原话时保留英文原文，另给中文翻译

**两阶段产出**：
- **v0.9** — 只有讲义、无转录。正常出笔记，frontmatter 标 `transcript: pending`（将来会有）或 `transcript: none`（那节课没录、永远不会有），第 9 节写明缺口
- **v1.0** — 转录到位后合并，补齐 🎙️ 内容、升级考点可信度，frontmatter 改 `transcript: merged`

**不要为了等转录而不写笔记。** v0.9 本身有用，合并成本很低。

---

## 6. 笔记骨架（9 节，固定）

| 节 | 标题 | 要求 |
|---|---|---|
| 0 | 三分钟速览 | 一句话概括 + 3~5 条**可检验**的「学完你能……」 |
| 1 | 开始之前 · 知识衔接 | 1.1 你已有的（表格：概念 + 一句话唤醒 + 回链）／1.2 本讲全新概念（标注在哪节展开）／1.3 为什么这一讲放在这里 + 重排说明 |
| 2 | 正文 | 按学习逻辑重排。每个概念走七格微结构（见下） |
| 3 | 一图看懂 | Mermaid 图或 Markdown 表格。画结构、谱系、流程、对比 |
| 4 | 速查表 | 考前一页纸。表格形式 |
| 5 | 双语术语卡 | `中文 \| English \| 考试可用的英文定义 \| 首现位置` |
| 6 | 考点预判与答题框架 | 三级可信度（教授明示 > ILO 反推 > 笔记推断）+ 答题模板 |
| 7 | 自测 | 概念题 + 案例分析题。答案用 `<details><summary>答案</summary>…</details>` 折叠 |
| 8 | 讲义页码映射 | `笔记小节 ↔ 讲义页码` 双向对照 |
| 9 | 延伸与勘误 | 9.1 课件有但课上略过／9.2 课上讲了但课件没有／9.3 课件自身的错误与口径不一致／9.4 课外补充 |

**正文里每个概念的七格微结构（不要省略任何一格）：**

```
是什么           — 定义，先给最短的，再给严谨的
为什么需要它     — 不讲清动机，读者记不住
课件原例         — 引用讲义的例子，标页码
🎙️ 课堂补充      — 教授实际讲的、课件上没有的（无转录时写「待转录补充」，别删这格）
💡 换个说法      — 你补的通俗类比/例子，必须标注是补充
常见误解         — 初学者最容易搞错的地方
与其他概念的关系 — 放进知识网，双链
```

---

## 7. 写作规范

**语言**
- 中文为主体
- 术语首次出现写 `中文（English Term）`
- 考试用英文的课程，关键定义**保留英文原句**再给中文翻译——考场上要默写的是英文
- 中文与英文/数字之间留空格

**忠实性（最重要的一条）**

必须区分并显式标注**四种来源**：

| 来源 | 标注方式 |
|---|---|
| 讲义原文/原例 | 直接引用 + `（讲义 p.XX）` |
| 课堂转录 | `🎙️ 课堂补充` + 时间戳（若有） |
| 你补充的解释、类比、例子 | `💡 换个说法（笔记补充）` |
| 外部资料 | `🔗` + 来源 + 获取日期 |

- **不编造**。材料里没有的，不要写成材料里有。不确定就写"课件此处未展开"
- 课件有错、口径不一致、或是明显的模板占位内容，**如实写进第 9 节**。读者需要知道哪些页信噪比低，好分配复习时间。粉饰课件对读者没有任何好处

**格式**
- 文件头写 YAML frontmatter：`course / module / week / date / source / transcript / prerequisites / new_concepts / tags / status / updated`
- 库内引用用 `[[双链]]`（Obsidian 语法）
- **公式一律 LaTeX**（`$…$` / `$$…$$`），绝不用代码块或行内代码写公式——Obsidian 不会渲染，读者看到的是乱码。表格里的竖线写 `\mid`。细则见 [[笔记模板]]「★ 公式与代码的写法」
- **每个小节写给第一次见这个概念的人**：先人话再定义、术语就地解释、公式配符号表和数字例子、代码配逐行说明、不以公式/代码/表格开头。细则见 [[笔记模板]]「★ 预习可读性」，验收见 [[对抗自检清单]] 第 11 项
- **⚠️ 质量硬门槛（2026-09-16 起）**：笔记标 `v0.9` 之前必须跑 `_meta/tools/note_quality.py <笔记> --strict --sections`，**L 合规 0 问题 + G 硬门槛全过**（§2 ≥ 250 字/内容页、一节 ≤ 1.6 个术语、≤ 2.5 页、首元素段落、公式符号有着落、有公式必有数字例子、代码三件套、「所以呢」覆盖 ≥ 0.80），新写 / 整篇重写的笔记还要加 frontmatter `quality_spec: v1` 并过 E 元素清单；`_meta/tools/link_check.py` 全库 0 问题。规则、阈值与校准依据见 [[笔记质量规范]]。**起因**：IS6400 M03 / M04 通过了全部既有检查却明显不如 M02 透彻——形式检查抓不住"薄"，透彻度必须量
- **⚠️ 两条链接硬规则（2026-09-10 全库返工 251 + 407 处后定下）**：
  1. **同名文件必须带目录**。五门课各有一份 `00-课程总览` / `术语表` / `考点库` / `作业与DDL` / `知识层级台账` / `课程前置资料` / `数据集卡片`，裸写 `[[术语表]]` 在 Obsidian 里会随机落到别的课。写法：`[[Business_Data_Analytics/_meta/术语表|术语表]]`（路径 + 别名，显示不变）。只有根目录文件（`CLAUDE` / `README` / `术语总表` / 各规则文件）可以裸写。
  2. **标题锚点写标题原文，不写 GitHub 式 slug**。Obsidian 按标题文字匹配，`#2-5-2-四条假设` 这种连字符写法**一个都匹配不上**（会静默跳到文件顶部）。写法：`[[M02-预测分析-线性回归#2.5.2 四条假设（讲义 p.17–18，⭐ 必考）|M02 §2.5.2]]`——锚点复制标题全文（去掉 `#` `|` `^` `[` `]`），别名给读者看。表格单元格里管道要转义成 `\|`。
  - 自检脚本见 [[对抗自检清单]] 第 9b 项。
- 图表用 Mermaid 或 Markdown 表格**重画**，不要嵌入位图截图——重画的图可搜索、可编辑、可被 AI 读懂
- 文件命名：`M01-主题.md`（两位数字，便于排序）

---

## 8. 材料处理

不同类型的材料有不同的提取方式和产出物，规则集中在 `_meta/材料处理规则.md`。

**当前已定义**：讲义 PDF、课程大纲 PDF、PPTX、DOCX、**课堂录音转录**。
**预留槽位（规则待定）**：`.csv/.xlsx` 数据文件、`.py/.ipynb` 代码文件、原始音视频、网页/论文。

遇到**尚未定义规则的材料类型**时：先在 `材料处理规则.md` 里补一节规则（提取方式 / 产出物形态 / 质量检查点），再开始处理。不要临场即兴决定，那会导致同类材料在不同 module 里处理方式不一致。

---

## 9. 工作流

1. 读该课 `_prep/课程前置资料.md` — 明确 ILO 和考核方式，决定笔记侧重
2. 读 `_meta/知识层级台账.md` — 分出"已有 / 新增 / 预告"三类概念
3. 通读全部讲义（**不要只读前几页就开始写**）
4. 纯图片页要设法看图补内容；看不了就在第 9 节注明"第 X 页为图片，未纳入"
5. 处理转录（若有）：按 `.claude/skills/transcript-merge/SKILL.md` 全流程（scan → 通读对齐 → 四态回填 → 结构块 → 跨文件回写 → audit PASS）
6. 定学习逻辑顺序，记录页码映射
7. 按 9 节骨架写正文，七格微结构
8. 回写：台账 L1、课程术语表、`_meta/术语总表.md`、考点库；有作业登记到 `作业与DDL.md`
9. 更新 Notion 进度看板（字段见 `_meta/Notion进度看板.md`）；没有 Notion 访问权就把该更新的内容列出来交给用户手动填
10. **★ 对抗自检**：跑 `_meta/对抗自检清单.md` 的 **12 项 + 11c 质量量表**（`_meta/tools/note_quality.py` G 硬门槛全过、`link_check.py` 0 问题），**全过才算完成**；任一 ✗ 停在 `draft`

---

## 9.1 对抗自检（写完不等于完成）

**写完一篇笔记后，换一个身份——一个想证明这篇笔记不合格的审查者——回头攻击自己的产出。**

完整清单见 `_meta/对抗自检清单.md`，四组共 12 项：

| 组 | 查什么 | 关键项 |
|---|---|---|
| **A · 覆盖** | 用脚本验，不许目测 | ① **每页都在正文有讲解**（不只在映射表）② 课上略过 ≠ 不写 ③ 低文本页全部视觉复核 ④ OLE 嵌入对象已提取 |
| **B · 真实性** | 最容易出事 | ⑤ **无转录时不许出现"教授说"** ⑥ 每条来源可回溯 ⑦ **无转录时置信度 🟡 封顶** ⑧ 数字全部可核 |
| **C · 连贯性** | | ⑨ 无悬空概念 ⑩ 台账已回写 |
| **D · 目标达成** | 最重要 | ⑪ **随机抽 3 页做预习可用性抽测** ⑫ **反方视角**：最弱的一节是哪节 / 哪个考点我答不上 / 哪些是推断 |

**为什么需要这一步**：本库已发生过的失败都有同一个形状——**"看起来做完了"**。

| 事故 | 后果 |
|---|---|
| 跳过纯图片页 | 漏掉整篇论文 + 两张关键表 |
| 19 页只进映射表 | 预习者学不到，而那是期中考内容 |
| 把"有备注字段"当"有备注内容" | 得出 106/117，实际 6/117 |
| 把"时间戳连续"当"录音完整" | 漏判开头 15 分钟永久缺失 |
| 按文本长度判定分隔页 | 差点漏掉整页理论出处 |

⚠️ **交付时不许写"已全部检查通过"了事——报数字、报页码、报结论。**

---

## 10. 扩展接口

- **加新课程**：建课程目录（英文名，下划线分隔）+ 完整子结构，先写 `_prep/课程前置资料.md` 再动笔记
- **加新材料类型**：只改 `_meta/材料处理规则.md`，加状态表一行 + 规则一节
- **改笔记骨架**：改 `_meta/笔记模板.md`，并在 `_meta/设计规格-笔记体系.md` 记录理由。旧笔记不强制回改

---

## 11. 硬约束

- **绝不移动、改名、删除或修改任何原始课件文件**。它们是唯一事实来源
- 中间产物（提取的文本、下载的文件）不要留在 vault 里
- 回复用户时用**简体中文**
- 拿不准的地方问用户，不要猜了就往下写
